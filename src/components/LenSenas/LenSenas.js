import React from 'react';
import { LenKeyboard } from '../LenKeyborar/LenKeyboard';
import { DivKeyboard } from './lensenasStyled';

export const LenSenas = () => {
    return (
        <DivKeyboard>
            <LenKeyboard />

        </DivKeyboard>
    )
}
